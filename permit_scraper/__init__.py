"""
permit_scraper — Agentic early-intelligence system for commercial building permits.

Quick start:
    from permit_scraper.pipeline import PermitPipeline

    pipeline = PermitPipeline()
    summary = pipeline.run(county_ids=["miami_dade"], days_back=14)
"""
