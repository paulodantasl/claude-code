.DS_Store
__pycache__/
*.py[cod]
*.pyo
.env
*.db
*.sqlite3
*.db-shm
*.db-wal
credentials.json
token.json

# Estimating: keep the structure & READMEs, never commit real plans/specs/pricing
estimating/projects/*
!estimating/projects/README.md


# Plugin-install project convention — must never appear in this repo (client data)
estimating-projects/

# permit_scraper live outputs (demo copies live in permit_scraper/demo/)
permit_scraper/output_*.csv
